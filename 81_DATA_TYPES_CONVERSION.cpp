#include<iostream>
#include <string>

using namespace std;

int main(){
	string s1;
	char* s2;
	int a = stoi(s1); // string to int
	int b = atoi(s2); // string to int
	double c = stod(s1); // string to double
	float d = stof(s1); // string to float
	string s3 = itos(a); // int to string

	return 0;
}